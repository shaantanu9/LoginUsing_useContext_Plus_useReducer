// import { useReducer, useState } from "react";

// function Child() {
//   // const [isAuth, dispatch] = useState("Old is Auth");

//   const reducer = (state, action) => {
//     switch (action.type){
//       case "c1":
//         return

//     }
//   };

//   const [isAuth, dispatch] = useReducer(reducer, "Old is Auth");

//   return (
//     <>
//       <p>{isAuth}</p>
//       <button onClick={() => dispatch("New is Auth")}>change isAuth</button>
//     </>
//   );
// }

// export default Child;
